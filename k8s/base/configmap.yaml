apiVersion: v1
kind: ConfigMap
metadata:
  name: kk-payments-config
  labels:
    app: kk-payments
data:
  NODE_ENV: production
  APP_PORT: "3001"
  LOG_LEVEL: info
  AWS_REGION: us-east-1
  DB_PORT: "5432"
  MAX_CONNECTIONS: "20"
  # Overridden per overlay: DB_HOST, RECEIPTS_BUCKET
