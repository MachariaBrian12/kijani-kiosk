apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kijani-staging

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: configmap-patch.yaml

images:
  - name: kijanikiosk/kk-payments
    newName: kijanikiosk/kk-payments
    newTag: "1.1.0"
