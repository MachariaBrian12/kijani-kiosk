# DOCUMENTATION ONLY — create imperatively, never commit real values.
#
# kubectl create secret generic kk-payments-secrets \
#   --from-literal=DB_PASSWORD='<password>' \
#   --from-literal=STRIPE_API_KEY='<stripe-key>' \
#   --from-literal=JWT_SECRET='<jwt-secret>' \
#   -n kijani-staging
#
# Repeat for -n kijani-project (production).
